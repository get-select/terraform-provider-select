# Generated files
openapi.public.json
openapi.v2.json
providers-schema.json

# Some processes build the provider and leave the binary in this dir
terraform-provider-select
# `go build ./tools/specpatch` leaves its binary here. Anchored, so it does not
# also exclude the tools/specpatch source directory.
/specpatch

# Terraform files
terraform.tfstate
terraform.tfstate.backup
.terraform/
.terraform.lock.hcl

# Go build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# terraformrc for local development
.terraformrc
.env.dev

tmp.md